# 判断推理辅导 Agent 设计文档

## 概述

为公务员考试备考应用的判断推理模块添加 AI 辅导功能，帮助用户提升判断推理解题能力。

## 设计目标

1. **错题诊断**：分析用户在判断推理模块的错题，找出薄弱点和错因
2. **题目生成**：根据用户水平生成判断推理练习题
3. **解题指导**：提供判断推理的解题技巧和策略
4. **进步评估**：评估用户在判断推理模块的进步情况

## 架构设计

### 组件

- `PanduanTutor`：AI Service 接口，绑定所有工具
- `PanduanDataTools`：数据访问工具，查询本地数据库
- 复用现有工具：`webSearchTool`、`pageFetchTool`

### 数据流

```
用户输入 → PanduanTutor → 工具调用 → 响应生成 → 用户
```

## AI Service 接口设计

```java
@AiService(tools = {"panduanDataTools", "webSearchTool", "pageFetchTool"})
public interface PanduanTutor {

    @SystemMessage("""
        你是判断推理模块的金牌辅导专家，拥有十年公考教学经验。
        你的任务是帮助用户提升判断推理模块的解题能力。

        ## 能力范围
        1. **错题诊断**：分析用户的错题，找出薄弱点和错因
        2. **题目生成**：根据用户水平生成新的练习题
        3. **解题指导**：提供判断推理的解题技巧和策略
        4. **进步评估**：评估用户在判断推理模块的进步情况

        ## 工作流程
        ### 错题诊断
        1. 调用 getRecentErrorDetails 获取用户在判断推理模块的错题
        2. 分析错题，识别错因类型：
           - 知识盲区：缺乏相关知识点
           - 粗心陷阱：会做但选了高频干扰项
           - 做题超时：会做但耗时过长
        3. 输出结构化诊断报告

        ### 题目生成
        1. 调用 getKnowledgeProfile 获取用户在判断推理模块的熟练度
        2. 根据熟练度水平，生成适合的练习题
        3. 题目类型包括：图形推理、定义判断、类比推理、逻辑判断
        4. 输出格式：JSON 数组，每道题包含 title、optionsJson、answer、analysis、category、concept

        ### 解题指导
        1. 根据用户的问题，提供针对性的解题技巧
        2. 技巧应具体、可操作，包含示例
        3. 可以调用 webSearchTool 搜索粉笔网的解题技巧

        ### 进步评估
        1. 调用 getKnowledgeProfile 获取用户的历史数据
        2. 分析用户在判断推理模块的进步趋势
        3. 输出进步报告，包含：当前水平、进步幅度、下一步建议

        ## 输出格式
        - 使用中文回复
        - 语言专业但通俗易懂
        - 结构化输出，便于前端展示
    """)
    String tutor(@P("用户消息") String message);
}
```

## 工具设计

### PanduanDataTools

```java
@Component("panduanDataTools")
public class PanduanDataTools {

    @Tool("获取用户在判断推理模块的最近错题明细")
    public List<ErrorDetail> getRecentErrorDetails(
        @P("用户ID") Long userId,
        @P("获取数量，默认10条") int limit) {
        // 查询数据库，返回判断推理模块的错题
    }

    @Tool("获取用户在判断推理模块的熟练度画像")
    public KnowledgeProfile getKnowledgeProfile(@P("用户ID") Long userId) {
        // 查询数据库，返回判断推理模块的熟练度数据
    }
}
```

### 复用现有工具

- `webSearchTool`：搜索粉笔网的判断推理题目和解题技巧
- `pageFetchTool`：抓取粉笔网的页面内容

## 前端交互设计

### 混合交互方式

- **对话区域**：用户可以自由输入问题，Agent 根据上下文回答
- **快捷按钮**：
  - "诊断我的错题"：触发错题诊断流程
  - "生成练习题"：触发题目生成流程
  - "解题技巧"：触发解题指导流程
  - "查看进步"：触发进步评估流程

### 界面布局

```
+----------------------------------+
|         判断推理辅导              |
+----------------------------------+
|                                  |
|         对话区域                 |
|                                  |
+----------------------------------+
| [诊断错题] [生成题目] [解题技巧] [查看进步] |
+----------------------------------+
|         输入框                   |
+----------------------------------+
```

## 数据库设计

### 新增表

**panduan_progress**：记录用户在判断推理模块的进步数据

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户ID |
| date | DATE | 记录日期 |
| accuracy | DECIMAL(5,2) | 总体正确率 |
| category_accuracy | JSON | 各类别正确率 |
| created_at | DATETIME | 创建时间 |

### 索引

- `idx_user_date`：(user_id, date) 用于查询用户历史数据

## 安全考虑

1. **输入验证**：验证用户输入，防止恶意注入
2. **权限控制**：确保用户只能访问自己的数据
3. **速率限制**：限制 API 调用频率，防止滥用
4. **数据隐私**：用户答题数据仅用于辅导，不用于其他用途

## 测试策略

1. **单元测试**：测试工具类的方法
2. **集成测试**：测试 Agent 与工具的集成
3. **端到端测试**：测试完整的用户交互流程

## 未来扩展

1. **更多模块**：将辅导功能扩展到其他模块（言语理解、数量关系等）
2. **个性化推荐**：根据用户学习习惯推荐题目
3. **学习计划**：生成个性化学习计划
4. **模拟考试**：提供模拟考试功能
