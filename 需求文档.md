📝 国考 AI 助教：流式问答模块需求文档
1. 需求背景
为 GK-Project 首页注入 AI 能力，通过模拟“真人专家”实时对话，解决学生在行测、申论备考过程中的即时疑问。
2. 优化后的功能特性
• 角色设定：静默注入系统提示词，确保 AI 始终以“十年国考专家”身份回答。
• 流式交互：后端采用 SSE 技术转发大模型响应，前端实现逐字显示的打字机效果。
• 富文本渲染：支持 Markdown 语法（表格、列表、加粗、代码块），确保公考知识点的排版清晰。
• 上下文约束：如果用户偏离公考话题，AI 将礼貌性地引导回备考主题。
• 异常处理：处理网络中断、API 超时、Token 溢出等异常，并给予用户友好提示。
3. 技术方案
3.1 后端方案 (gk-backend)
• 技术栈：Spring Boot + Spring WebFlux (或 SseEmitter)。
• 核心逻辑：
  1. 封装请求体，包含用户消息和预设的 System Prompt。
  2. 调用大模型 API（如 OpenAI、智谱 AI、DeepSeek 等），设置 stream: true。
  3. 通过 Flux 或 ResponseBodyEmitter 将接收到的每个数据块实时推送到前端。
3.2 前端方案 (vue-project)
• 技术栈：Vue 3 (Composition API) + fetch API。
• 核心逻辑：
  1. 使用 fetch 获取响应流（ReadableStream）。
  2. 使用 TextDecoder 解码二进制流。
  3. 引入 markdown-it 将实时增长的字符串渲染为 HTML。
  4. 引入 github-markdown-css 提供美观的样式。
---
4. 核心实现步骤
第一步：后端注入 System Prompt
在 gk-backend 新建 ChatController.java。
Java
// 伪代码示例
@GetMapping(value = "/api/chat/stream", produces = MediaType.TEXT_EVENT_STREAM_VALUE)
public Flux<String> chatStream(@RequestParam String userPrompt) {
    // 1. 构造消息列表
    List<Message> messages = new ArrayList<>();
    // 2. 强制静默注入 System Prompt
    messages.add(new Message("system", "你现在是一位拥有十年教龄的国家公务员考试金牌辅导专家..."));
    messages.add(new Message("user", userPrompt));
    
    // 3. 调用模型 API (以 WebClient 为例)
    return webClient.post()
        .uri("/v1/chat/completions")
        .bodyValue(new ChatRequest(messages, true))
        .retrieve()
        .bodyToFlux(String.class); // 转发流
}
第二步：前端读取流并渲染
在 vue-project 的 HomeView.vue 中处理输入。
JavaScript
import MarkdownIt from 'markdown-it';
const md = new MarkdownIt();
const chatResult = ref('');
const displayHtml = computed(() => md.render(chatResult.value));

async function handleSend() {
  const response = await fetch(`/api/chat/stream?userPrompt=${userInput.value}`);
  const reader = response.body.getReader();
  const decoder = new TextDecoder();
  
  while (true) {
    const { done, value } = await reader.read();
    if (done) break;
    const chunk = decoder.decode(value, { stream: true });
    // 解析并追加内容
    chatResult.value += parseContent(chunk); 
  }
}
---
为了让你更直观地理解 System Prompt 注入 和 SSE 流式传输 的协同工作原理，我准备了一个交互式演示工具：

显示可视化演示



实施建议：
1. Markdown 样式：建议安装 github-markdown-css，并在渲染容器上添加 markdown-body 类名，这样生成的表格和代码块会自动拥有精美的样式。
2. 自动滚动：随着 AI 回复变长，你需要监听 chatResult 的变化，并自动将聊天窗口滚动到底部（使用 nextTick 结合 scrollTop）。
3. API 选型：如果你在寻找国内低延迟的 API，可以考虑 DeepSeek 或 智谱 AI，它们的 SSE 支持非常标准且文档齐全。